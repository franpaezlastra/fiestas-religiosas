# Desarrollo local: rutas relativas → proxy Vite → API preview
VITE_API_URL=/api/v1

# Cloudinary (URLs de fotos cuando el API público aún no serializa `media.url`)
VITE_CLOUDINARY_CLOUD_NAME=duuwqmpmn

# Build / DonWeb (secreto GitHub Actions VITE_API_URL):
# https://api-preview.fiestasreligiosas.com/api/v1

# Backend local sin proxy:
# VITE_API_URL=http://localhost:3000/api/v1
